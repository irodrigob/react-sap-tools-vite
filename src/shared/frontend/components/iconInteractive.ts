import { styled } from "@mui/material/styles";
import { Icon } from "@ui5/webcomponents-react";

const IconInteractive = styled(Icon)({
  cursor: "pointer",
  interactive: true,
});

export default IconInteractive;
